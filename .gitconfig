[user]
    name = Dmitrii Senin
    email = seninds@gmail.com
[alias]
    co = checkout
    ci = commit
    st = status
    br = branch
    hist = log --pretty=format:'%C(yellow)%ad%C(reset) %C(green)%h%C(reset) | %C(red)%s %C(bold red)[%an]%C(reset) %C(blue)%d%C(reset)' --graph --date=short
    alias = ! git config --list | grep ^alias | cut -c 7-
